services:
  - type: static_site
    name: dry-fruits-frontend
    buildCommand: "npm install && npm run build"
    publishDir: "dist"
    routes:
      - type: rewrite
        source: /*
        destination: /index.html